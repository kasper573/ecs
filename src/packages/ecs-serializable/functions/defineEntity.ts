import { RedefinableEntity } from "../RedefinableEntity";
import { EntityDefinition } from "../types/EntityDefinition";

export const defineEntity = (definition: EntityDefinition) => RedefinableEntity;
